# backend_challenge

Run 
``` shell
docker-compose build
```

``` shell
docker-compose run web rake db:create
docker-compose run web rake db:migrate
```

``` shell
docker-compose up
```

And it is working